/**
 * Backward-compatible re-export.
 * All components importing from this file continue to work unchanged.
 * The actual implementation lives in src/context/AuthContext.tsx.
 */
export { useAuth } from '../context/AuthContext';
export type { Role } from '../types';
